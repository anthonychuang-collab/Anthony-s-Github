# 如何更新到新版(不會弄丟已排的班)

你所有的資料(帳號、人員、班表、規則設定)都存在資料夾裡的 **`data/`**。
只要換程式、**不要動 `data/`**,資料就會完整保留。

---

## 最簡單的做法:只換「程式」,保留「資料」

假設你舊版資料夾叫 `線東排班系統`,新版 zip 解壓後叫 `線東排班系統_最新`。

### 步驟
1. **先把系統關掉**(關掉那個黑色視窗)。
2. 打開**舊的**資料夾,把裡面的 **`data/` 資料夾**複製起來(這是你的全部資料)。
3. 進到**新的**資料夾 `線東排班系統_最新`,如果它裡面已經有一個空的 `data/`,
   把它刪掉,再把**步驟 2 複製的舊 `data/`** 貼進來。
4. 連點兩下新資料夾裡的 **`啟動排班系統.command`**。
   → 第一次會重新安裝元件(1~3 分鐘),完成後打開就是新版 + 你原本的資料。

> 一句話版:**把舊的 `data/` 搬到新資料夾,蓋掉新的空 `data/`,然後從新資料夾啟動。**

---

## 更省事的做法:直接蓋檔

如果你不想搬 `data/`,也可以反過來——**把新版的「程式檔」蓋到舊資料夾上**:

1. 關掉系統(關黑色視窗)。
2. 打開新版 zip,把裡面**除了 `data/` 以外**的東西
   (`public/`、`server/`、`package.json`、`啟動排班系統.command` 等)
   全部複製,貼到你**舊的**資料夾,選「**取代/合併**」。
   → 因為沒有動到舊資料夾的 `data/`,資料原封不動。
3. 一樣連點兩下 `啟動排班系統.command` 啟動。

---

## 保險起見:換版前先備份

換之前,建議先把**整個舊資料夾**複製一份、改名成像 `線東排班系統_備份_0826`。
萬一新版有任何不對,雙擊備份資料夾裡的啟動器,就能立刻回到舊版 + 舊資料。

---

## 怎麼確認換成功了?

進系統後到「⚙後台管理 → 班別代碼維護」,如果**內建代碼那排看得到「顏色」下拉選單**,
就是新版了(舊版沒有這個)。班表下方也會多一排可點擊的**代碼參考列**。

有任何一步卡住,把畫面截圖給我。
