# 線東大眾護理之家 排班系統 — Mac 使用說明

給第一次用的人，照著做就會動。全程大約 5 分鐘。

---

## 第一次啟動（只需做一次的準備）

### 步驟 1：確認電腦有沒有 Node.js
本系統需要一個免費的小工具「Node.js」。

- 如果不確定有沒有裝，**直接跳到步驟 2 連點兩下啟動器**，
  它會自動檢查。若沒裝，畫面會告訴你怎麼裝。
- 要自己先裝也可以：到 <https://nodejs.org/>，
  下載左邊綠色的「**LTS**」按鈕，一路按「繼續／安裝」即可。

### 步驟 2：啟動系統
在這個資料夾裡，找到 **`啟動排班系統.command`**，**連點兩下**。

> 🔒 第一次 macOS 可能會擋，說「無法確認開發者」。
> 這是正常的安全提醒。解法：
> **對著 `啟動排班系統.command` 按右鍵 → 選「打開」→ 再按一次「打開」。**
> 之後每次連點兩下就能直接開。

會跳出一個黑色視窗，第一次它會自動安裝元件（1～3 分鐘），
完成後**瀏覽器會自動打開**系統畫面。

### 步驟 3：建立你的管理者帳號
第一次打開網頁，系統會請你設定**第一個管理者帳號**（帳號＋密碼）。
設定好就能登入開始排班了。

---

## 之後每次要用

只要**連點兩下 `啟動排班系統.command`**，等瀏覽器自動打開即可。

- 要**結束系統**：把那個黑色視窗關掉，或在視窗裡按 `Control + C`。
- 資料都存在這個資料夾的 `data/` 裡，關掉不會不見。

---

## 常見問題

**Q：瀏覽器沒有自動打開？**
自己打開瀏覽器，網址列輸入： `http://localhost:3000`

**Q：其他同事的電腦也要看排班？**
只要在**同一個 Wi-Fi／區網**，他們的瀏覽器輸入：
`http://<你這台電腦的IP>:3000`
（你的 IP 在「系統設定 → 網路」可以看到，通常長得像 192.168.x.x）

**Q：黑色視窗出現「埠號 3000 已經被占用」或「EADDRINUSE」？**
代表系統**已經在另一個黑色視窗執行中**了(通常是你之前開過、還沒關)。
處理方式:把**所有**黑色視窗都關掉,再連點兩下啟動器**一次**就好;
或直接開瀏覽器輸入 `http://localhost:3000` 就會看到系統。
（新版啟動器已經會自動偵測:如果系統已在跑,會直接幫你打開瀏覽器,不會再出現這個錯誤。）
**同一時間只要開一個系統視窗就好。**

**Q：黑色視窗顯示「找不到 Node.js」？**
照視窗裡的指示去 <https://nodejs.org/> 裝好 LTS 版，關掉視窗再連點兩下一次。

**Q：想搬到另一台電腦？**
把整個資料夾複製過去即可（含 `data/` 資料夾就會連同排班資料一起搬）。
新電腦第一次啟動一樣會自動安裝元件。

---

有任何一步卡住，把黑色視窗裡的訊息截圖給我，我幫你看。
